<%= simple_form_for Holiday.new do |form| %>

	<%= form.input :url, maxlength: false %>
	<%= form.input :notifications %>
	
	<%= form.button :submit, "Start tracking this holiday", data: { disable_with: "Grabbing holiday ..." } %>

<% end %>